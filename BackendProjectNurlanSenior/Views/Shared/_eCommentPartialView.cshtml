@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@model EventVM
@foreach (Comment item in Model.Event.Comments)
{
    if (item.IsAccepted == true)
    {
        <div id="commentdiv">
            <h5>@item.AppUser.Fullname</h5>
            <span>@item.CreatedTime</span>

            <span><strong>@item.Subject</strong></span>
            <br />
            <input type="hidden" value="@item.Id" id="hiddenId" class="hiddenClass" />
            <p>@item.Message</p>
            @if (item.AppUser.UserName == User.Identity.Name)
            {
                <a   style="text-decoration:none" class="text text-danger deleteCmt">DELETE</a>
            }

        </div>

    }

}
